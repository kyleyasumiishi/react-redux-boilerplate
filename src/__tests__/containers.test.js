import React from "react";
import { shallow, configure, mount } from "enzyme";
import Adapter from "enzyme-adapter-react-16";

// Import containers
// Import components

configure({ adapter: new Adapter() });

// Test that components render the correct components

describe("containers", () => {});
